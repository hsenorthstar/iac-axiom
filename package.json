{
  "name": "iac-axiom",
  "version": "1.0.0",
  "description": "IAC (Axiom) — real full-stack: Express + SQLite API with a thin web client. One source of truth, the same rows for web and app.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "reset": "node server/seed.js --reset"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  }
}
